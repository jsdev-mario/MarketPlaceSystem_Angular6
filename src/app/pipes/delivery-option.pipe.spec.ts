import { DeliveryOptionPipe } from './delivery-option.pipe';

describe('DeliveryOptionPipe', () => {
  it('create an instance', () => {
    const pipe = new DeliveryOptionPipe();
    expect(pipe).toBeTruthy();
  });
});
