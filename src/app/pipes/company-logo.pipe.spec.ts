import { CompanyLogoPipe } from './company-logo.pipe';

describe('CompanyLogoPipe', () => {
  it('create an instance', () => {
    const pipe = new CompanyLogoPipe();
    expect(pipe).toBeTruthy();
  });
});
